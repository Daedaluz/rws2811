[package]
name = "rws2811"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.29", features = ["ioctl", "time", "signal", "event"] }
clap = { version = "4", features = ["derive"] }
